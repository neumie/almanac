# Codex Setup

To use almanac skills with Codex, symlink the skills directory:

```bash
ln -s /path/to/almanac/skills ~/.agents/skills/almanac
```

Skills in `SKILL.md` format are natively discovered by Codex.
