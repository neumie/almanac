#!/usr/bin/env bash
# session-stop — Async hook that logs session end (runs in background)

ALMANAC_ROOT="${ALMANAC_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
LOG_DIR="$ALMANAC_ROOT/.logs"
mkdir -p "$LOG_DIR"

echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) session ended" >> "$LOG_DIR/sessions.log"
