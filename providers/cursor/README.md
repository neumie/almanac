# Cursor Setup

To use almanac skills with Cursor, symlink the skills directory:

```bash
ln -s /path/to/almanac/skills ~/.cursor/skills/almanac
```

Skills in `SKILL.md` format are natively discovered by Cursor.
