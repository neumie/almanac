# OpenCode Setup

To use almanac skills with OpenCode, symlink the skills directory:

```bash
ln -s /path/to/almanac/skills ~/.config/opencode/skills/almanac
```

Skills in `SKILL.md` format are natively discovered by OpenCode.
